<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hams">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@raw/icon"
        android:label="@string/app_name"
        android:roundIcon="@raw/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".studentAccount" />
        <activity android:name=".StudentReport" />
        <activity android:name=".StudentRegister" />
        <activity android:name=".wardenAccount" />
        <activity android:name=".wardenSList" />
        <activity android:name=".wardenReport" />
        <activity android:name=".attendance" />
        <activity android:name=".studentHome" />
        <activity android:name=".wardenHome" />
        <activity android:name=".register"/>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>